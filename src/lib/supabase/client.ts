// Placeholder Supabase client setup.
// No keys, passwords, or secrets live in this file or anywhere in this repo.
// Values are read at runtime from environment variables you set locally in
// .env.local (see .env.local.example) and, in production, in your hosting
// provider's environment settings — never committed to git.
//
// Once you're ready to wire up Supabase, run:
//   npm install @supabase/supabase-js
// and uncomment the code below.

// import { createClient } from "@supabase/supabase-js";
//
// const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL!;
// const supabaseAnonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!;
//
// export const supabase = createClient(supabaseUrl, supabaseAnonKey);

export {};
